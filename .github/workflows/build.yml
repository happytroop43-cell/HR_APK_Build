name: Build Standalone APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code Files
      uses: actions/checkout@v4

    - name: Install System Architecture Tool Chains
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libssl-dev cmake libffi-dev libgdbm-dev bzip2 libsqlite3-dev python3-pip python3-setuptools python3-virtualenv

    - name: Set Up Python Platform Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Deploy Buildozer Engine Packages
      run: |
        pip install --upgrade pip
        pip install buildozer cython virtualenv

    - name: Compile Application Layout into Standalone APK
      run: |
        buildozer -v android debug

    - name: Export Output Package Artifact
      uses: actions/artifacts@v4
      with:
        name: Compiled-HR-System-APK
        path: bin/*.apk
